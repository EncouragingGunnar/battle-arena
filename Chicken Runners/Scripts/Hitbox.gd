extends Area2D
class_name Hitbox

#hitbox klass, exporterar in damage knockback strength och en frivillig hiteffect

export (int) var damage
export (int) var knockback_strength
export (PackedScene) var hiteffect
